# 🌐 URL Shortener DNS - Flask + IONOS API

Un proyecto que convierte URLs largas en registros DNS TXT automáticos usando la API de IONOS.
Cada enlace se guarda como un subdominio con un hash único, y puedes consultarlo con dig.

